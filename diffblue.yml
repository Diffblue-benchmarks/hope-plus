cbmcArguments:
  # Do not generate coverage comments on generated tests.
  java-generate-no-comments: true
phases:
  -
  #Phase 0 PRETTY AND SIMPLE - Simple models, quick cover, pretty Tests, Do Not Test Private Method Directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [ private ]
      # Instrument the entry point function only.
      cover-function-only: false
      # Instrument the file as opposed to the function only
      cover-only: file
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
  -
  #Phase 1 PRETTY AND COMPLEX - Standard models, longer run time, pretty tests, Do Not Test Private Method Directly
    timeout: 180
    cbmcArguments:
      # Use the standard models library
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 2
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [ private ]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
  -
  #Phase 2 Simple and Private Directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
      # If the logs contain output of the following then proceed to phase X
    nextPhase:
      # If the function is not analysable then do not pass onto the next phase
      not_analyzed: null
  -
  #Phase 3 Complex and Private Directly
    timeout: 180
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 3000
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 3
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
  -
  #Phase 4 EXCEPTIONS
    timeout: 180
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: true
      # Set an upper bound on loop unwinding.
  -
    #PHASE 5 Big numbers and less contraints on inputs 
    timeout: 300
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 100
      # Bound the length of input strings.
      max-nondet-string-length: 100
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 6
units:
  # Target Coverage: Unknown %
  - target:
    - dir: hope-core/target/classes
      file: com/hope/service/impl/SysResourceServiceImpl.class
      sourceDir: hope-core/src/main/java
      sourceFile: com/hope/service/impl/SysResourceServiceImpl.java
    # Child of SysResourceServiceImpl.class
    # Child Coverage: Unknown %
    - dir: hope-core/target/classes
      file: com/hope/model/dto/Resource.class
      sourceDir: hope-core/src/main/java
      sourceFile: com/hope/model/dto/Resource.java
    # Child of SysResourceServiceImpl.class
    # Child Coverage: Unknown %
    - dir: hope-core/target/classes
      file: com/hope/model/beans/SysRole.class
      sourceDir: hope-core/src/main/java
      sourceFile: com/hope/model/beans/SysRole.java
    # Child of SysResourceServiceImpl.class
    # Child Coverage: Unknown %
    - dir: hope-core/target/classes
      file: com/hope/model/beans/SysResource.class
      sourceDir: hope-core/src/main/java
      sourceFile: com/hope/model/beans/SysResource.java
    # Child of SysResourceServiceImpl.class
    # Child Coverage: Unknown %
    - dir: hope-core/target/classes
      file: com/hope/service/SysResourceService.class
      sourceDir: hope-core/src/main/java
      sourceFile: com/hope/service/SysResourceService.java
    # Child of SysResourceServiceImpl.class
    # Child Coverage: Unknown %
    - dir: hope-framework/target/classes
      file: com/hope/mybatis/service/impl/BaseServiceImpl.class
      sourceDir: hope-framework/src/main/java
      sourceFile: com/hope/mybatis/service/impl/BaseServiceImpl.java
    # Child of SysResourceServiceImpl.class
    # Child Coverage: Unknown %
    - dir: hope-core/target/classes
      file: com/hope/mapper/SysResourceMapper.class
      sourceDir: hope-core/src/main/java
      sourceFile: com/hope/mapper/SysResourceMapper.java
  # Target Coverage: Unknown %
  - target:
    - dir: hope-core/target/classes
      file: com/hope/service/impl/SysRoleServiceImpl.class
      sourceDir: hope-core/src/main/java
      sourceFile: com/hope/service/impl/SysRoleServiceImpl.java
    # Child of SysRoleServiceImpl.class
    # Child Coverage: Unknown %
    - dir: hope-core/target/classes
      file: com/hope/mapper/SysRoleMapper.class
      sourceDir: hope-core/src/main/java
      sourceFile: com/hope/mapper/SysRoleMapper.java
    # Child of SysRoleServiceImpl.class
    # Child Coverage: Unknown %
    - dir: hope-core/target/classes
      file: com/hope/model/dto/Role.class
      sourceDir: hope-core/src/main/java
      sourceFile: com/hope/model/dto/Role.java
    # Child of SysRoleServiceImpl.class
    # Child Coverage: Unknown %
    - dir: hope-core/target/classes
      file: com/hope/service/SysRoleService.class
      sourceDir: hope-core/src/main/java
      sourceFile: com/hope/service/SysRoleService.java
    # Child of SysRoleServiceImpl.class
    # Child Coverage: Unknown %
    - dir: hope-core/target/classes
      file: com/hope/model/beans/SysRole.class
      sourceDir: hope-core/src/main/java
      sourceFile: com/hope/model/beans/SysRole.java
    # Child of SysRoleServiceImpl.class
    # Child Coverage: Unknown %
    - dir: hope-core/target/classes
      file: com/hope/model/vo/RoleConditionVo.class
      sourceDir: hope-core/src/main/java
      sourceFile: com/hope/model/vo/RoleConditionVo.java
    # Child of SysRoleServiceImpl.class
    # Child Coverage: Unknown %
    - dir: hope-framework/target/classes
      file: com/hope/object/ResponseVo.class
      sourceDir: hope-framework/src/main/java
      sourceFile: com/hope/object/ResponseVo.java
    # Child of SysRoleServiceImpl.class
    # Child Coverage: Unknown %
    - dir: hope-core/target/classes
      file: com/hope/model/beans/SysRoleResource.class
      sourceDir: hope-core/src/main/java
      sourceFile: com/hope/model/beans/SysRoleResource.java
    # Child of SysRoleServiceImpl.class
    # Child Coverage: Unknown %
    - dir: hope-framework/target/classes
      file: com/hope/mybatis/service/impl/BaseServiceImpl.class
      sourceDir: hope-framework/src/main/java
      sourceFile: com/hope/mybatis/service/impl/BaseServiceImpl.java
    # Child of SysRoleServiceImpl.class
    # Child Coverage: Unknown %
    - dir: hope-core/target/classes
      file: com/hope/mapper/SysRoleResourceMapper.class
      sourceDir: hope-core/src/main/java
      sourceFile: com/hope/mapper/SysRoleResourceMapper.java
    # Child of SysRoleServiceImpl.class
    # Child Coverage: Unknown %
    - dir: hope-framework/target/classes
      file: com/hope/utils/ResultHopeUtil.class
      sourceDir: hope-framework/src/main/java
      sourceFile: com/hope/utils/ResultHopeUtil.java
